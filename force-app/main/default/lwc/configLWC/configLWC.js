import { api, LightningElement } from 'lwc';

export default class ConfigLWC extends LightningElement {

       @api recordId
       @api age
       @api levels
       @api heading
       @api height
} 
